<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.android_register_system">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Android_Register_System"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".CourseListActivity"
            android:label="@string/title_activity_course_list"
            android:theme="@style/Theme.Android_Register_System.NoActionBar"></activity>
        <activity android:name=".RegisterDetailActivity" />
        <activity android:name=".TeacherCourseDetailActivity" />
        <activity android:name=".AddCourseActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".TeacherMainActivity" />
        <activity android:name=".StudentMainActivity" />
        <activity
            android:name=".StudentCourseDetailActivity"
            android:hardwareAccelerated="true" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>